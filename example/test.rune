person = type{}
person.name = "person"
person2 = new(person)
println(typeof(person), " " , person) # map {name: person}
println(typeof(person2), " " , person2) # map {name: person}

person2.name = "person2"
println(typeof(person), " " , person) # map {name: person2}
println(typeof(person2), " " , person2) # map {name: person2}