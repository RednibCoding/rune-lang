myvar = 12

toPrint = "I am the toPrint variable"

printer = fun(printme) {
    println(printme)
}

if myvar == 10 then
    printer("then")
elif myvar == 12 then
    printer("elif 12\n")
elif myvar == 11 then
    printer("elif 11")
else printer("else")

while myvar > 0 {
    myvar = myvar  -1
    printer(myvar)
}

if myvar < 0 {
    printer("Is smaller than 0")
} elif myvar > 0 {
    printer("Is biger or than 0")
} else {
    printer("Is exactly 0")
    printer("Is exactly 0 the second time")
}

if true then printer("Nice") else printer("Also nice")

runeIsAwesome = true

if runeIsAwesome then println("Rune is awesome") elif runeIsAwesome != true then println("Oh to bad") else println("This should never print! :)")

if runeIsAwesome {
    println("Rune is awesome")
} elif runeIsAwesome != true {
    println("Oh to bad")
} else {
    println("This should never print! :)")
}

println(typeof(1)) # int
println(typeof(10.1)) # float
println(typeof("Hello")) # "string"
println(typeof(false)) # bool

myArr = array{"hello", 10, false, 20, 30}
first = myArr[0]
println(myArr[0])
println(myArr[1])
println(myArr[2])
println(myArr[3])
println(myArr[4])

println(len(myArr))
myStr = "Hi "
myStr = append(myStr, " you")
println(myStr)

myMap = map{"key1": 1, "key1": false}

second = myMap["pennor2"]
println(second)
println(typeof(myMap))

println(myMap)
myMap = append(myMap, "penno3", 10.3)
println(myMap)
myMap = append(myMap, "penno3", "hey")
println(myMap)

if (haskey(myMap, "penno3")) {
    println("haskey")
} else {
    println("has not key")
}
myMap = remove(myMap, "penno3")
println(myMap)